<Window x:Class="LoginSystem.CourseManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Course Management" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnAdd" Content="Add Course" Width="100" Height="30" Margin="0,0,10,0" Click="btnAdd_Click"/>
            <Button x:Name="btnEdit" Content="Edit Course" Width="100" Height="30" Margin="0,0,10,0" Click="btnEdit_Click"/>
            <Button x:Name="btnDelete" Content="Delete Course" Width="100" Height="30" Click="btnDelete_Click"/>
        </StackPanel>

        <DataGrid x:Name="dgCourses" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Course Code" Binding="{Binding CourseCode}" Width="120"/>
                <DataGridTextColumn Header="Course Name" Binding="{Binding CourseName}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300"/>
                <DataGridTextColumn Header="Credits" Binding="{Binding Credits}" Width="80"/>
                <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" x:Name="btnBack" Content="Back" Width="100" Height="30" HorizontalAlignment="Right" Margin="0,10,0,0" Click="btnBack_Click"/>
    </Grid>
</Window> 